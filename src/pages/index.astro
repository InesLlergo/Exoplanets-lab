---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Bienvenido a Exoplanet Lab">
  <style>
    .welcome-container {
      background: rgba(17,24,39,0.7);
      backdrop-filter: blur(16px);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 1.5rem;
      padding: 3rem 4rem;
      max-width: 600px;
      width: 90%;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }

    h1 {
      font-size: 2.5rem;
      color: #f9fafb;
      margin: 0;
    }

    .subtitle {
      font-size: 1rem;
      color: #9ca3af;
      margin-bottom: 1.5rem;
    }

    .button {
      display: inline-block;
      background: linear-gradient(to right, #4f46e5, #6366f1);
      color: #fff;
      border: none;
      border-radius: .75rem;
      padding: .8rem 2.5rem;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      text-decoration: none;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      box-shadow: 0 4px 15px rgba(79,70,229,0.4);
    }

    .button:hover {
      transform: scale(1.05);
      box-shadow: 0 6px 20px rgba(79,70,229,0.5);
    }
  </style>

  <div class="welcome-container">
    <h1 data-key="title">🚀 Bienvenido a Exoplanet Lab</h1>
    <p class="subtitle" data-key="subtitle">Prepárate para explorar y aprender sobre exoplanetas.</p>
    <a href="/menu" class="button">Comenzar</a>
  </div>
</Layout>