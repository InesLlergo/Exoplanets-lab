---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Aventura Espacial">
    <style>
        :root { --font-main: 'Poppins', system-ui, sans-serif; }

        body {
            margin: 0;
            font-family: var(--font-main);
            background: radial-gradient(circle at center, #0f172a, #020617);
            color: #fff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        section {
            display: none; /* Oculto por defecto */
            width: 100%;
            height: 100vh;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            text-align: center;
            padding: 2rem;
            box-sizing: border-box; 
        }

        .visible { display: flex; } /* Clase para mostrar una sección */

        .menu-container, .adventure-container, .card {
            background: rgba(17,24,39,0.7);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 1.5rem;
            padding: 2rem 3rem;
            max-width: 960px;
            width: 90%;
            text-align: center;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: #f9fafb;
        }

        .subtitle {
            font-size: 1rem;
            color: #9ca3af;
            margin-bottom: 2rem;
        }

        .modes {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 2rem;
        }

        /* La tarjeta es ahora un enlace (<a>), se asegura que no tenga estilos de link */
        a.mode-card {
            text-decoration: none;
            color: inherit;
        }

        .mode-card {
            background: rgba(31,41,55,0.7);
            border-radius: 1.2rem;
            padding: 1.5rem;
            width: 280px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
        }

        .mode-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(79,70,229,0.3);
        }

        .mode-card .image {
            width: 100%;
            height: 160px;
            border-radius: 1rem;
            margin-bottom: 1rem;
            background-size: cover;
            background-position: center;
        }

        .learning { background-image: url('https://images.unsplash.com/photo-1612282415801-9b43e9432c56?fit=crop&w=600&q=60'); }
        .lab { background-image: url('https://images.unsplash.com/photo-1614727834218-8b0b0f2cc1b0?fit=crop&w=600&q=60'); }

        button {
            background: linear-gradient(to right,#4f46e5,#6366f1);
            color:#fff;
            border:none;
            border-radius:.75rem;
            padding:.8rem 1.5rem;
            font-size:1rem;
            font-weight:600;
            cursor:pointer;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            box-shadow:0 4px 15px rgba(79,70,229,0.4);
        }

        button:hover {
            transform: scale(1.05);
            box-shadow:0 6px 20px rgba(79,70,229,0.5);
        }
        
        .lang-toggle {
            position: absolute;
            top: 2rem;
            right: 2rem;
            padding: 0.5rem 1rem;
            background: rgba(31,41,55,0.7);
        }

        .back-btn {
            margin-top: 2rem;
            background: linear-gradient(to right, #16a34a, #22c55e);
            box-shadow:0 4px 15px rgba(22, 163, 74, 0.4);
        }
        .back-btn:hover {
             box-shadow:0 6px 20px rgba(22, 163, 74, 0.5);
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />

    <section id="startMenu" class="visible">
        <div class="menu-container">
            <button class="lang-toggle">EN/ES</button>
            <h1 data-key="title">🚀 Bienvenido a Exoplanet lab</h1>
            <p class="subtitle" data-key="subtitle">Prepárate para explorar y aprender sobre exoplanetas.</p>
            <button id="startButton">Comenzar</button>
        </div>
    </section>

    <section id="modeSelectionMenu">
        <div class="adventure-container">
             <h1>Elige tu Aventura</h1>
             <p class="subtitle">Selecciona un modo para comenzar tu viaje.</p>
             <div class="modes">
                  <div class="mode-card">
                      <div class="image learning"></div>
                      <h3>Modo Aprendizaje</h3>
                      <p>Descubre datos fascinantes sobre planetas lejanos.</p>
                  </div>
                  <a href="/lab" class="mode-card">
                      <div class="image lab"></div>
                      <h3>Laboratorio Interactivo</h3>
                      <p>Experimenta con las condiciones de diferentes mundos.</p>
                  </a>
             </div>
             <button id="backButton" class="back-btn">Regresar al Menú</button>
        </div>
    </section>

    <script>
        // Seleccionar los elementos del DOM
        const startMenu = document.querySelector('#startMenu');
        const modeSelectionMenu = document.querySelector('#modeSelectionMenu');
        
        const startButton = document.querySelector('#startButton');
        const backButton = document.querySelector('#backButton');

        // Evento para el botón "Comenzar"
        startButton.addEventListener('click', () => {
            startMenu.classList.remove('visible'); // Oculta el menú de inicio
            modeSelectionMenu.classList.add('visible'); // Muestra el menú de selección
        });

        // Evento para el botón "Regresar"
        backButton.addEventListener('click', () => {
            modeSelectionMenu.classList.remove('visible'); // Oculta el menú de selección
            startMenu.classList.add('visible'); // Muestra el menú de inicio
        });
    </script>
</Layout>